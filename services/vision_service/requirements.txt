# Vision service - API layer
# Pins match what is already installed in the repo .venv.

fastapi==0.141.1
uvicorn==0.52.4
websockets>=13            # required by uvicorn for WebSocket support

# Vision pipeline
ultralytics==8.4.129
opencv-python==5.0.0.93
torch==2.11.0
torchvision==0.26.0
numpy==2.5.2

# Persistence
sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.5

# Resumen de incidentes con IA. El SDK unificado de Google (equivalente en
# Python del @google/genai que fija STACK.md), no el antiguo
# google-generativeai. La clave va en el .env, nunca en el código.
google-genai==2.22.0

# Support
pydantic==2.13.4
python-multipart==0.0.32
